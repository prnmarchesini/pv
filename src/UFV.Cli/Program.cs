namespace UFV.Cli;

/// <summary>
/// Executavel de linha de comando que roda o motor em lote (JSON entra, JSON sai).
/// Na etapa 0 ele so responde, para provar que a solucao compila e executa.
/// </summary>
public static class Program
{
    public static int Main(string[] args)
    {
        Console.WriteLine("UFV.Cli pronto.");
        return 0;
    }
}
